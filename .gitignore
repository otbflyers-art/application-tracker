__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/

# The tracker workbook and run log are tracked in git — the GitHub Actions
# workflow commits updates to them after every run. Only the local-only
# `check` command's seen-links cache is left untracked.
ib_checker_seen.json
